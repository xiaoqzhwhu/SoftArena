__pycache__/
*.py[cod]
.DS_Store

runs/
datasets/
softarena/registry/*_index.generated.json

# Toolize is a large external tool corpus. Keep it outside the first
# SoftArena source commit; wire it as a submodule or documented dependency.
toolize/
models/
.tmp_video_frames/
.tmp_video_frames_jpg/

# External benchmark checkouts are local dependencies, not vendored source.
external/
