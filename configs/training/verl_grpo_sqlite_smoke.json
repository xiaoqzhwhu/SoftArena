{
  "recipe_id": "verl_grpo_sqlite_smoke_v1",
  "method": "grpo",
  "trainer": "verl",
  "base_model": "Qwen/Qwen2.5-7B-Instruct",
  "dataset": "datasets/sft/sqlite_smoke.jsonl",
  "output_dir": "models",
  "hyperparameters": {
    "epochs": 1,
    "learning_rate": 1e-06,
    "batch_size": 8,
    "max_prompt_length": 4096,
    "max_response_length": 2048
  },
  "metadata": {
    "purpose": "Prepare a verl GRPO launch using SoftArena verifier scores as reward smoke signal."
  }
}
